services:
  bot:
    build: .
    restart: unless-stopped
    env_file: .env
    dns:
      - 8.8.8.8
      - 1.1.1.1
    ports:
      - "8080:8080"
    volumes:
      - bot-data:/data

volumes:
  bot-data:
